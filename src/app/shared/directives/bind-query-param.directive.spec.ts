import { BindQueryParamDirective } from './bind-query-param.directive';

describe('BindQueryParamDirective', () => {
  it('should create an instance', () => {
    const directive = new BindQueryParamDirective();
    expect(directive).toBeTruthy();
  });
});
